/**
 * The supported libraries in patron.
 * @enum {Symbol}
 * @readonly
 */

const Libraries = {
  /** The eris library */
  Eris: Symbol(),
  /** The discord.js library */
  DiscordJS: Symbol()
};

module.exports = Libraries;
